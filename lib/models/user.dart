class User {
  String id;
  String username;
  String email;
  bool isEmailVerified;

  User({
    required this.id,
    required this.username,
    required this.email,
    required this.isEmailVerified,
  });
}
